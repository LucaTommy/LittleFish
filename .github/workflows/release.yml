# Releases are now created locally via:  python build.py publish
# This workflow is kept as a no-op to avoid confusion if tags are pushed.
# The publish command builds locally and uploads to GitHub Releases directly.

name: Build & Release (disabled)

on:
  workflow_dispatch:  # manual only, not triggered by tags

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Releases are built locally. Use 'python build.py publish'."
